# Dependencies for the AlertsApiFunction Lambda function.
# boto3/botocore intentionally excluded — provided by the Lambda runtime.
#
# No third-party dependencies: SMS alerts are sent via Amazon SNS using the
# boto3 client from the runtime (the Twilio dependency was removed when alerts
# migrated from Twilio WhatsApp to Amazon SNS SMS).
